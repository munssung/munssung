using System;
using static System.Console;

namespace Bool;

class MainApp
{
    static void Main(string[] args)
    {
        bool a = true;
        bool b = false;

        WriteLine(a);
        WriteLine(b);
    }
}